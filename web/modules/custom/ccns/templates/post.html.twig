<div>
  {% if logged_in %}
    Soon you will be able to start a topic with a link, a title and an optional text. <br />
    When there is no link, the text will be required and will appear on top of the thread.
  {% else %}
    Please <a href="{{ path('ccns.login_modal') }}" class="use-ajax" id="nostr-auth">login</a> first.
  {% endif %}
</div>
